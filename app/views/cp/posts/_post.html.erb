<div class="post">
	<h2><%= post.title %></h2>
	<div class="info">posted on <%= pretty_date(post.created_at, :time => true) %>. <%= link_to "edit", edit_cp_show_post_path(@show, post), :class => 'edit-link' %></div>
	<div class="content"><%= display_message(post.body) %></div>
</div>